%% Clean Up
close all; clear; clc;

%% Integration

%Declaring function handles
func1 = @EOM1;
func2 = @EOM2;

%Time span
tspan = [0 5];

%Initial Condit



%% Functions

function dydt = EOM1(t, y)
    dydt = -t*y / sqrt(2 - y^2);
end

function outVec = EOM2(t, state)
    x1 = state(1);
    x2 = state(2);
    
    dx1dt = x2;
    dx2dt = 0.5*(5 + x1^2 - x2*exp(ex1));
    
    outVec = [dx1dt; dx2dt];
end