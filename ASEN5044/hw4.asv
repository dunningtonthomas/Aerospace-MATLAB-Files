%% Clean
close all; clear; clc;


%% Problem 1
total_number_hands = nchoosek(52,2);


sum = 0;
sum1 = 0;
for i = 1:10000
    xi = rand() - 0.5;
    xj = rand() - 0.5;
    xk = rand() - 0.5;
    xl = rand() - 0.5;

    sum(i) = (xi + xj) / 2;
    sum1(i) = (xi + xj + xk + xl) / 4;
end


